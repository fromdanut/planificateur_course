{# app/Resources/views/nav-menu.html.twig #}
<div id="custom-bootstrap-menu" class="navbar navbar-default " role="navigation">
    <div class="container-fluid">
        <div class="navbar-header"><a class="navbar-brand" href="#">PlanCourses</a>
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-menubuilder"><span class="sr-only">Toggle navigation</span><span class="icon-bar"></span><span class="icon-bar"></span><span class="icon-bar"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse navbar-menubuilder">
            <ul class="nav navbar-nav navbar-left">
                <li><a href="{{path('pc_platform_homepage')}}">Home</a></li>

                {# 
                    disabled menu items with href=# links if user isn't logged

                    A terme faire 2 menu de nav ? logged, not logged ?
                #}

                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <li><a href="{{path('pc_platform_recipe_index')}}">My recipes</a></li>
                    <li class="dropdown">
                      <a class="btn dropdown-toggle" type="button" data-toggle="dropdown">Shopping list
                      <span class="caret"></span></a>
                      <ul class="dropdown-menu">
                        <li><a href="{{path('pc_platform_shoppinglist_add')}}">Create a shopping list</a></li>
                        <li><a href="#">My shopping list</a></li>
                      </ul>
                    </li>
                    <li class="dropdown">
                        <a class="btn dropdown-toggle" type="button" data-toggle="dropdown"><span class="glyphicon glyphicon-user"></span></a>
                            <ul class="dropdown-menu">
                                {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                                <p class="text-center">{{ app.user.username }}</p>
                                <li class="divider"></li>
                                <li><a href="{{path('pc_platform_shoppinglistoption_edit')}}">Options</a></li>
                                <li><a href="{{ path('fos_user_change_password') }}">Modifier le mot de passe</a></li>
                                <li><a href="{{ path('fos_user_security_logout') }}">Déconnexion</a></li>
                                {% else %}
                                <li><a href="{{ path('fos_user_security_login') }}">Connexion</a></li>
                                <li><a href="{{ path('fos_user_registration_register') }}">S'inscrire</a></li>
                                {% endif %}
                            </ul>
                        </li>
                {% else %}
                    <li class="disabled"><a href="#">My recipes</a></li>
                    <li class="disabled"><a href="#">Shopping list</a></li>
                    <li class="disabled"><a href="#"><span class="glyphicon glyphicon-user"></span></a></li>
                {% endif %}

            </ul>
        </div>
    </div>
</div>
